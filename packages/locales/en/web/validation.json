{
  "string": {
    "minLength": "Value must have minimum {{ min }} characters",
    "maxLength": "Value must have maximum {{ max }} characters",
    "trim": "Value must be a trimmed string"
  },
  "number": {
    "minLength": "Value must be {{ min }} or greater",
    "maxLength": "Value must be {{ max }} or less"
  },
  "array": {
    "minLength": "Value must have minimum {{ min }} elements",
    "maxLength": "Value must have maximum {{ max }} elements"
  },
  "mixed": {
    "required": "Field is required",
    "notType": "Field must have type {{ type }}",
    "oneOf": "Field value must be one of: {{ values }}"
  }
}
